{
  "name": "yasenka",
  "version": "3.8.0",
  "private": true,
  "description": "Yasenka — браузерный питомец-помощник (Chrome extension). package.json только для тестов: зависимостей нет.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
